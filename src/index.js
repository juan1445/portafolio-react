import React from 'react';
import ReactDOM from 'react-dom';
import About from './components/About';

ReactDOM.render( 
    //Que voy a renderizar (El componente)
    <About />,
    // Donde lo voy a renderizar (id)
     document.getElementById('app')
);